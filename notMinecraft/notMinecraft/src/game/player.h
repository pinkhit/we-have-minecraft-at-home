#pragma once

#include <../src/graphics/camera.h>


// player controller: input & movement & interaction
class player
{
public:
	player(void);
	~player(void);

	void updateControls(void);

private:
	// camera belongs to player
	
	// collision resolution
};